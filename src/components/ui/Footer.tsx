import { Separator } from "@/components/ui/separator"

export const Footer = () => {
    return(
        <footer className="mt-6">
            <Separator />
            <p className="my-5 text-center opacity-50 text-sm">Feito por Arthur Gabriel</p>
        </footer>
    )
}